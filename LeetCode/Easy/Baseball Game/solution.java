class Solution {
    public int calPoints(String[] operations) {
        Stack<Integer> stk = new Stack<>();
        int len = operations.length;
        for(int i = 0;i<len;i++){
            
        }
    }
}